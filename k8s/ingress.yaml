apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stellar-viaduct-ingress
  namespace: stellar-viaduct
  labels:
    app.kubernetes.io/name: stellar-viaduct
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/use-regex: "true"
    # TLS/SSL redirect
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # WebSocket support
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - stellar-viaduct.example.com
      secretName: stellar-viaduct-tls
  rules:
    - host: stellar-viaduct.example.com
      http:
        paths:
          # Backend API and WebSocket
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: stellar-viaduct-backend
                port:
                  number: 3001
          - path: /metrics
            pathType: Prefix
            backend:
              service:
                name: stellar-viaduct-backend
                port:
                  number: 3001
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: stellar-viaduct-backend
                port:
                  number: 3001
          # Frontend (catch-all)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stellar-viaduct-frontend
                port:
                  number: 3000
